package com.miya.web.config;

import org.springframework.context.annotation.Configuration;

/**
 * Spring Security配置
 */
@Configuration
public class SecurityConfig {
    // Security-related configuration has been removed to avoid compile errors when
    // Spring Security is not present on the classpath. Add the appropriate
    // Spring Security dependency in your build (pom.xml or build.gradle) and
    // restore the bean if you need to enable security.
}
